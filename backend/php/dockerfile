FROM php:8.4-apache

# Install mysqli (and pdo_mysql if you might use PDO later)
RUN docker-php-ext-install mysqli pdo_mysql && docker-php-ext-enable mysqli

# Optional: enable Apache rewrite module if you plan to use clean URLs
RUN a2enmod rewrite